import React from 'react';
import Para from '../../../shared/para/para';
import { BI, I } from '../../../shared/util/util';

const ReactDefinition = () => {
  return (
    <Para>
      React is a JavaScript library for building <BI>user interfaces</BI>. React
      helps to build encapsulated components that manage their own <I>state</I>,
      then compose them to make complex UIs.
    </Para>
  );
};

export default ReactDefinition;
